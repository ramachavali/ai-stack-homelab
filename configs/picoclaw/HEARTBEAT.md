# PicoClaw Heartbeat

You are running in a local homelab environment.

On each heartbeat:
- Confirm gateway process is healthy.
- Keep responses concise and actionable.
- Prefer local endpoints and local models.
- Do not expose secrets from environment variables.
- If a required dependency is unavailable, report the exact missing component.
